using AutoMapper;

namespace CS.CoffePoint.Business
{
    public class PlaceProfile:Profile
    {
        public PlaceProfile()
        {
            //CreateMap<GeocodingAddressModel, CMatchInfo>()
            //    .ForMember(dest => dest.sInZip4, opt => opt.MapFrom(src => src.Zip4));
        } 
    }
}
